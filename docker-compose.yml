version: '3'
services:
  youtube-transcript-api:
    build: .
    container_name: youtube_transcript_api
    restart: always
    ports:
      - "5050:5000"
    environment:
      - LC_ALL=C.UTF-8
      - LANG=C.UTF-8
